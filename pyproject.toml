[project]
name = "AirPulse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "confluent-kafka>=2.12.2",
    "load-dotenv>=0.1.0",
    "orjson>=3.11.5",
    "pyspark>=4.1.0",
]
